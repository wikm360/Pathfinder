# 🗺️ مسیریاب هوشمند گراف

یک پروژه تحت وب با Flutter که الگوریتم‌های مسیریابی مختلف را روی گراف‌های تصادفی به صورت بصری نمایش می‌دهد.

## ✨ ویژگی‌ها

- **ایجاد گراف تصادفی**: امکان ساخت گراف با تعداد نودهای دلخواه (3 تا 30 نود)
- **5 الگوریتم مسیریابی**:
  - **BFS** (Breadth-First Search) - جستجوی سطح به سطح
  - **DFS** (Depth-First Search) - جستجوی عمق محور
  - **UCS** (Uniform Cost Search) - جستجوی با هزینه یکنواخت
  - **ID** (Iterative Deepening) - جستجوی عمقی تکراری
  - **A*** - جستجوی هیوریستیک
- **نمایش گام به گام**: مشاهده فرآیند جستجو با انیمیشن
- **تنظیم سرعت انیمیشن**: کنترل سرعت نمایش مراحل
- **رابط کاربری زیبا و مدرن**: با پشتیبانی از حالت تاریک
- **ریسپانسیو**: قابل استفاده در موبایل، تبلت و دسکتاپ

## 🎯 نحوه استفاده

1. **تعداد نودها را وارد کنید** (بین 3 تا 30)
2. روی دکمه "ایجاد گراف جدید" کلیک کنید
3. **نود شروع** را با کلیک انتخاب کنید (سبز رنگ می‌شود)
4. **نود هدف** را با کلیک انتخاب کنید (قرمز رنگ می‌شود)
5. **الگوریتم** مورد نظر را از منو انتخاب کنید
6. روی دکمه "اجرا" کلیک کنید و مشاهده کنید!

## 🎨 راهنمای رنگ‌ها

- 🟢 **سبز**: نود شروع
- 🔴 **قرمز**: نود هدف
- 🟠 **نارنجی**: نود در حال بررسی
- 🟡 **زرد**: نودهای در صف/استک
- ⚪ **خاکستری**: نودهای بررسی شده
- 🟢 **سبز روشن**: مسیر نهایی

## 📁 ساختار پروژه

```
lib/
├── models/              # مدل‌های داده
│   ├── node.dart       # کلاس نود
│   ├── edge.dart       # کلاس یال
│   ├── graph.dart      # کلاس گراف و تولید تصادفی
│   └── search_result.dart  # نتیجه جستجو
│
├── algorithms/         # الگوریتم‌های مسیریابی
│   ├── bfs.dart       # جستجوی سطحی
│   ├── dfs.dart       # جستجوی عمقی
│   ├── ucs.dart       # جستجوی با هزینه یکنواخت
│   ├── iterative_deepening.dart  # عمقی تکراری
│   └── astar.dart     # الگوریتم A*
│
├── widgets/           # کامپوننت‌های UI
│   └── graph_painter.dart  # رسم گراف
│
├── screens/           # صفحات اپلیکیشن
│   └── pathfinder_screen.dart  # صفحه اصلی
│
└── main.dart         # نقطه شروع برنامه
```

## 🔧 نصب و اجرا

### پیش‌نیازها
- Flutter SDK (نسخه 3.0 یا بالاتر)
- Dart SDK

### مراحل نصب

1. کلون کردن پروژه:
```bash
git clone <repository-url>
cd flutter_application_1
```

2. نصب وابستگی‌ها:
```bash
flutter pub get
```

3. اجرای پروژه (تحت وب):
```bash
flutter run -d chrome
```

یا برای سایر پلتفرم‌ها:
```bash
flutter run
```

## 📚 توضیح الگوریتم‌ها

### BFS (Breadth-First Search)
- از صف (Queue) استفاده می‌کند
- تمام نودهای یک سطح را قبل از رفتن به سطح بعدی بررسی می‌کند
- **تضمین**: کوتاه‌ترین مسیر (از نظر تعداد یال)
- **پیچیدگی**: O(V + E)

### DFS (Depth-First Search)
- از استک (Stack) استفاده می‌کند
- تا انتهای هر مسیر پیش می‌رود
- **توجه**: مسیر پیدا شده لزوماً کوتاه‌ترین نیست
- **پیچیدگی**: O(V + E)

### UCS (Uniform Cost Search)
- از صف اولویت‌دار استفاده می‌کند
- نودها را بر اساس کمترین هزینه انتخاب می‌کند
- **تضمین**: ارزان‌ترین مسیر
- **پیچیدگی**: O((V + E) log V)

### ID (Iterative Deepening)
- DFS را با محدودیت عمق مختلف اجرا می‌کند
- ترکیب مزایای BFS و DFS
- **تضمین**: کوتاه‌ترین مسیر با استفاده کم از حافظه
- **پیچیدگی**: O(b^d)

### A* (A-Star)
- از هیوریستیک برای راهنمایی جستجو استفاده می‌کند
- f(n) = g(n) + h(n)
  - g(n): هزینه واقعی از شروع
  - h(n): تخمین هزینه تا هدف (فاصله اقلیدسی)
- **تضمین**: بهینه‌ترین مسیر (با هیوریستیک admissible)
- **پیچیدگی**: بستگی به کیفیت هیوریستیک دارد

## 🎓 نکات آموزشی

این پروژه برای درس هوش مصنوعی طراحی شده و شامل:

1. **کد تمیز و خوانا**: با کامنت‌های فارسی
2. **معماری مدولار**: هر الگوریتم در فایل جداگانه
3. **نمایش بصری**: برای درک بهتر نحوه کار الگوریتم‌ها
4. **مقایسه عملکرد**: مشاهده تعداد نودهای بررسی شده

## 🚀 ویژگی‌های فنی

- **Flutter & Dart**: برای توسعه کراس‌پلتفرم
- **Custom Painter**: برای رسم گراف
- **State Management**: با استفاده از StatefulWidget
- **Responsive Design**: با استفاده از MediaQuery و LayoutBuilder
- **Material Design 3**: برای UI مدرن و زیبا

## 📱 پلتفرم‌های پشتیبانی شده

- ✅ Web (Chrome, Firefox, Safari, Edge)
- ✅ Android
- ✅ iOS
- ✅ Windows
- ✅ macOS
- ✅ Linux

## 🤝 مشارکت

این پروژه برای اهداف آموزشی ساخته شده است. برای بهبود:
1. Fork کنید
2. تغییرات خود را اعمال کنید
3. Pull Request بزنید

## 📄 مجوز

این پروژه تحت مجوز MIT است.

## 👨‍💻 سازنده

ساخته شده برای درس هوش مصنوعی با ❤️

---

**نکته**: برای نمایش بهتر فونت‌های فارسی، فونت Vazir به پروژه اضافه کنید یا از فونت پیش‌فرض سیستم استفاده می‌شود.
